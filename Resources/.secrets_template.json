{
  "api_keys": {
    "openai": "${OPENAI_KEY}",
    "google_search": "${GOOGLE_API_KEY}"
  },
  "databases": {
    "main": {
      "url": "${DB_URL}",
      "username": "${DB_USER}",
      "password": "${DB_PASS}"
    }
  }
}